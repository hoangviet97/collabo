import React from "react";

const ProjectStatus = (props) => {
  switch (props.children) {
    case "On Progress":
      break;

    default:
      break;
  }

  return (
    <>
      <span>{}</span>
    </>
  );
};

export default ProjectStatus;
